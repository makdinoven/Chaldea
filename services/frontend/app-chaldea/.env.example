# Frontend API URL configuration
# All values default to empty string (requests go through Nginx reverse proxy)
# Set these only if you need to point to a different backend

VITE_BASE_URL=
VITE_BASE_URL_DEFAULT=
VITE_BASE_URL_BATTLES=
VITE_BASE_URL_AUTOBATTLES=/autobattle
